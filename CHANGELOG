
Version 0.2.2 Changes

- Added check to see if spigot service is running to back it up and stop before starting SRSI
- Added various fixes to spinit (backup had syntax errors, so backup manually before running SRSI again)
- Added more prompts to sensitive operations for skipping (to use SRSI to reconfigure spinit)

WAS, Apr 9, 2016
Version 0.2.1 Changes

- Remove exit if server fails to start to finish up install

WAS, Apr 7, 2016
Version 0.2 Changes

- Removed cjava from init script which can cause complications on certain builds, and is a bit redundant.

